{
  "name": "getSurveyLambda",
  "version": "1.0.0",
  "description": "lambda function to get survey",
  "main": "index.js",
  "scripts": {
    "build": "zip -r getSurvey.zip .",
    "test": "source .env && node test.js",
    "test:demo": "source .env.demo && node test.js",
    "test:testing": "source .env.testing && node test.js"
  },
  "author": "rvillalba",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.18.0",
    "crypto-js": "3.1.6",
    "qs": "^6.5.2",
    "request": "^2.65.0"
  },
  "devDependencies": {
    "eslint": "^4.19.1"
  }
}
